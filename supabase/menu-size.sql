-- =============================================================
-- メニュー項目に「表示サイズ」を追加
--   1 = 小, 2 = 中(既定), 3 = 大
--   レジ画面でのボタンの大きさ・文字サイズに反映されます。
-- Supabase の SQL Editor で実行してください。
-- =============================================================

alter table public.menu_items
  add column if not exists size smallint not null default 2;
